# Sentence Construction

Simple sentence construction quiz made with React + Vite + Typescript.

## Running this Locally

Install the dependencies and start the `json-server`.

```
bun i
bun run server
```

Configure the BASE_URL in a `.env` file.

Then run the react + vite app:

```
bun run dev
```
